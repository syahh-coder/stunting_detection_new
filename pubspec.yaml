name: stunting_client
description: Stunting Detection - lightweight client
publish_to: "none"
version: 1.0.0+1

environment:
  sdk: ">=3.3.0 <4.0.0"

dependencies:
  flutter:
    sdk: flutter
  google_mlkit_pose_detection: ^0.10.0
  image_picker: ^1.1.1
  http: ^1.5.0
  supabase_flutter: ^2.5.0
dev_dependencies:
  flutter_test:
    sdk: flutter

flutter:
  uses-material-design: true
